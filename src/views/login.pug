extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST") 
        input(placeholder="Username", name="username", type="text", required)
        input(placeholder="Password", name="password", type="password", required)
        input(type="submit", value="Join")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=00b1938affa478773fa0&allow_signup=false") Continue Github &rarr;
        hr
        div 
            span 계졍이 없으십니까? 
            a(href="/login") Create one now &rarr;